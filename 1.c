
#include <stdio.h>

int main() 
  int x = 0, y = 0;
  char a[60];
  printf("enter a string");
  scanf("%[^\n]%*c",60[a]);
  if(x > 0)
    if(y > 0)
      printf("%s",str); 
  else
    printf("error in printing string"); 
}